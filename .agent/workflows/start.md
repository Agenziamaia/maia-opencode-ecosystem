---
description: ONE-CLICK LAUNCH: Wake Up + Health Check + Memory Verify + God Mode
---

# 🚀 SYSTEM STARTUP SEQUENCE

1. **Ignite Infrastructure** (Server, Kanban, Heartbeat)
   `./WAKEUP.sh`

2. **System Health & Memory Check** (15s Deep Scan)
   `python3 .opencode/scripts/health_check.py --ping --fix`

3. **Authorize God Mode & Init**
   *System is fully initialized. Memories are active. Copy the prompt below to authorize the session.*
   
   `cat DOCS/REVOLUTION_DAY_1_AUTHORIZATION.md`
